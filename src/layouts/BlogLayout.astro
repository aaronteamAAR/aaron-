---
import Layout from "./Layout.astro";
import Logo from "../components/Logo.astro";
import Header from "../components/Header.astro";
import '../styles/blog.css'
import ProgressTracker from "../components/ProgressTracker.astro";
// console.log(frontmatter)
export interface Props {
  content: {
    title: string;
    description: string;
    pubDate: string;
    readingTime: string;
  };
}
const {
  content: { title, description, pubDate, readingTime },
} = Astro.props;
---

<Layout title={title}>
  <Logo />
  <Header links={[
    {
    href: '/blog',
    title: "Blog"
},
{
    href: 'https://github.com/aaronteamAAR/',
    title: "Projects✨"
} ,
{
    href:'/',
    title: "go back !"
}
]
} />
  <ProgressTracker />
    <div class="desc flex flex-col items-center text-center">
        <h1 class="h1_text">{title}</h1>
        <p class="text-lg text-lightdark font-semibold">
          {description}
        </p>
        <p class="text-sm">{pubDate} - {readingTime}</p>
    </div>
    <div class="main-blog">
        <slot />
    </div>
    
</Layout>
